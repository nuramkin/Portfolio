Movie Library done in Web Application Version(for C# class)

Purpose: Web Application that stores movies in a database.  Each movie will have 
a name, description, runtime(length), and a status for whether or not it is owned.

You may need to deploy the database before you build and run it.